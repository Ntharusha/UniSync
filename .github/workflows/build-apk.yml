name: Build Mobile Android APK

on:
  push:
    branches:
      - main
      - dev
  workflow_dispatch:

jobs:
  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4